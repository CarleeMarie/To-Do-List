// you can use this to access the file system (fs), so you can write, retrieve, delete info from the db.json file

const uuid = require('../helpers/uuid');

const notes = [
    {
        "title":"Test Title",
        "text":"Test text"
    }
]